import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  marginHorizontal: {
    marginHorizontal: 5,
    marginVertical: 10,
    fontSize: 20,
    fontWeight: 'bold',
  },
});

export default styles;
