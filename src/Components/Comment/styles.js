import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    //flex: 1,
  },

  comentario: {
    fontSize: 14,
  },

  containerLegenda: {
    flexDirection: "row",
    alignItems: "center",
    paddingVertical: 10,
  },

  avatar: {
    height: 44,
    width: 44,
    borderRadius: 44 / 2,
    marginHorizontal: 15,
    backgroundColor: "#ddd",
  },
});

export default styles;
